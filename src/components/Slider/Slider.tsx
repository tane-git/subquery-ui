import React from "react";
import { Slider as AntSlider, SliderSingleProps } from "antd";

// const Button = (props: ButtonProps) => <AntButton {...props} />;

type TypeSlider = typeof AntSlider

// const Slider: TypeSlider = (props) => <AntSlider {...props} />;

const Slider = AntSlider

export { Slider };
